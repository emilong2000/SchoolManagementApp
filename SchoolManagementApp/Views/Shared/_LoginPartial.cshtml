@using Microsoft.AspNet.Identity

    @if (Request.IsAuthenticated)
    {


        <strong>@Html.Encode(User.Identity.Name) | </strong>

        <strong> @Html.ActionLink("LogOut", "Logout", "Home") </strong> }
    else
    {

        <strong>@Html.ActionLink("Log In", "Login", "Home")</strong>

    }


